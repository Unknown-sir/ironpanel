document.addEventListener('click',e=>{if(e.target.classList.contains('copy')){const id=e.target.dataset.copy; const el=document.getElementById(id); if(el){navigator.clipboard.writeText(el.innerText); e.target.innerText='کپی شد'; setTimeout(()=>e.target.innerText='کپی',1200)}}});
function setRing(name,val){const card=document.querySelector(`[data-metric="${name}"] .ring`); if(card){card.style.setProperty('--p', Math.max(0,Math.min(100,val))+'%'); card.classList.toggle('warning', val>=70);}}
async function refreshMetrics(){try{const r=await fetch('/api/system/metrics'); if(!r.ok)return; const m=await r.json(); [['cpu',m.cpu_percent],['ram',m.ram_percent],['swap',m.swap_percent],['disk',m.disk_percent]].forEach(x=>{const el=document.getElementById(x[0]+'_percent'); if(el) el.innerText=Math.round(x[1])+'%';setRing(x[0],x[1])}); document.getElementById('cpu_sub').innerText=m.cpu_freq+' GHz'; document.getElementById('ram_sub').innerText=m.ram_used_mb+'MB / '+m.ram_total_mb+'MB'; document.getElementById('swap_sub').innerText=m.swap_used_mb+'MB / '+m.swap_total_mb+'MB'; document.getElementById('disk_sub').innerText=m.disk_used_gb+'GB / '+m.disk_total_gb+'GB'; const ld=document.getElementById('license_days'); if(ld){let d=m.license_days_remaining; ld.innerText=(d===null||d===undefined)?'--':d; document.getElementById('license_sub').innerText=(d===null||d===undefined)?'نامشخص':('روز باقی‌مانده تا انقضا'); setRing('license', d===null||d===undefined?0:Math.min(100,Math.max(0,d))); const lr=document.querySelector('[data-metric="license"] .ring'); if(lr) lr.classList.toggle('warning', d!==null&&d!==undefined&&d<=7); }}catch(e){}}
if(document.querySelector('.system-monitor')){refreshMetrics(); setInterval(refreshMetrics,3000)}
