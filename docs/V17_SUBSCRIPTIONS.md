# v17 Subscription Outputs

Each user can receive multiple client-specific outputs:

- `/s/<token>/raw`
- `/s/<token>/clash`
- `/s/<token>/singbox`
- `/s/<token>/hiddify`

The raw feed remains clean and contains only importable links.
