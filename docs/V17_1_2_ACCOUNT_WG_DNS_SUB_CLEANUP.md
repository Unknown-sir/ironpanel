# v17.1.2 - Account, WireGuard DNS and Clean Subscription

- Added Account page for main admin and resellers to change their own username/password.
- Added reseller delete action for main admin with options to detach, disable or delete reseller-owned users.
- Added WireGuard DNS setting so generated WireGuard client configs no longer use a fixed DNS.
- Cleaned public subscription output: OpenVPN/WireGuard are download-only, Telegram Proxy is copy-only, Hysteria2 is shown as one profile, and SSH/PPTP show only connection fields.
