# v17 Node Agent

The node agent connects a remote server to the master panel using a per-node token. The agent sends CPU, RAM, disk, network counters, version and health status to `/api/v2/node/heartbeat`.

Create a node in the panel, open the generated install command and run it on the node server.
